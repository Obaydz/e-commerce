<form method="post" action="{{ path('app_key_delete', {'id': key.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ key.id) }}">
    <button class="btn">Delete</button>
</form>
